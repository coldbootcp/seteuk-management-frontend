# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

한국 고등학생(16~18세)이 유일한 사용자다. 학기 중에 데스크톱과 모바일 양쪽에서 쓴다.
학부모·교사·컨설턴트용 화면이나 역할은 없다 — 비로그인 랜딩을 포함한 모든 화면의
독자는 학생 한 명이다.

학생이 이 앱으로 하는 일은, 3년 동안 쌓이는 수십 개의 활동 기록이 서로 이어지도록
붙들고 가는 것이다. 지난 학기 보고서가 이번 학기 탐구로 어떻게 이어져야 하는지를
혼자 추적하기는 16세에게 어렵다.

모든 화면은 **16세가 혼자 읽고 판단할 수 있어야 한다.** 입시 용어를 이미 아는 성인을
기준으로 쓰지 않는다.

## Product Purpose

세특연구소는 대입 학교생활기록부의 '세부능력 및 특기사항'(세특)을 관리하는 AI 코치다.

입학사정관은 생기부를 하나의 이야기로 읽는다. 그런데 대부분의 학생에게는 개별 기록은
멀쩡해도 그 기록들을 잇는 **서사가 없다.** 이 앱은 흩어진 활동 기록을 입학사정관이
읽는 '하나의 연구 서사'로 엮도록 돕는다.

성공은 학생이 다음 활동을 고를 때 이전 기록에서 출발하고, 저장한 기록이 다시 다음
진단의 근거가 되는 상태다.

## Positioning

학교 테마를 씌운 챗봇이 아니다. 제품 전체가 **닫힌 루프 하나**다:

진단 → 계획 → 실행 → 기록 → 다시 진단

모든 화면은 이 루프의 한 단계이고, 루프를 붙들어 주는 것은 **activity lineage**다.
각 기록은 자신이 이어받은 이전 활동을 가리키고(`parent_activity_id`), 각 계획은 자신이
나온 기록을 기억한다. 그래서 범용 챗봇이 할 수 없는 질문 — "지난 학기에 지수 모델의
한계에 부딪혔는데, 로지스틱 모델로 더 밀어볼지 넓혀볼지" — 을 학생의 실제 기록에서
꺼낼 수 있다.

## Operating Context

- **학기 흐름에 맞춰 쓴다.** 수행평가·수업·동아리·대회처럼 학교에서 실제로 생긴 기회에
  맞춰 주제를 고르고, 활동을 마친 뒤 어떤 방식으로 했는지 직접 기록한다.
- **입구:** 가입 → 온보딩(생기부 PDF 업로드는 선택) → 진단 → 상담 대화 → 계획 확정.
  진단받지 않은 학생에게는 계획의 근거가 없으므로, 상담이 끝나기 전까지 계획·추천·
  일반 챗봇은 잠겨 있다. 상담 종료와 계획 확정은 **학생의 명시적인 버튼 입력으로만**
  일어나며, 챗봇이 스스로 결정하지 않는다.
- **자료:** 학생이 업로드한 생기부 PDF, 학생이 직접 입력한 활동·성적·시간표·계획·대화.
- **시스템 경계:** 이 리포는 화면만 담당한다. LLM 호출·DB·생기부 파싱은 전부 별도
  백엔드(FastAPI + PostgreSQL)에 있다. 경계 규칙은 `AGENTS.md`가 정본이다.

## Capabilities and Constraints

**화면(메인 앱 탭):** 이번 학기, 3개년 흐름, 대시보드, 성적 관리, 시간표, 캘린더,
활동 & 세특, 수시 포트폴리오, AI 컨설턴트(챗봇, 대화에서 바로 기록을 쓰는 편집 모드),
프로필 설정. 그 밖에 온보딩, 진단·상담 관문, 비로그인 랜딩, 로그인(Google),
이메일 인증·비밀번호 재설정, 계정 설정·탈퇴, 개인정보처리방침·이용약관.

**진로 범위:** 전 진로를 대상으로 한다. 반도체는 추천 품질을 먼저 검증하는 **첫 평가
파일럿**일 뿐 제품 범위의 한정이 아니다(`docs/semiconductor-pilot-v0.3.md`,
`docs/semiconductor-evaluation-set-v0.1.md`).

**용어:** 기록의 이름은 화면에서 **'생기부'**로 통일한다. 현재 코드에는 '학생부'가
섞여 있다(정리는 별도 작업). 공식 명칭을 풀어 써야 할 때만 '학교생활기록부'를 쓴다.
'세특'은 '세부능력 및 특기사항'의 줄임말로 쓴다.

**제품 제약:**

- **근거 없는 추천은 화면에 띄우지 않는다.** 모든 추천에는 그 학생의 구체적 근거와
  이유가 함께 보여야 한다. 생성된 추천은 후보이며, 검수 단계가 근거 없는·반복되는·
  안전하지 않은 출력을 걸러낼 수 있어야 한다.
- **입력칸을 잠그지 않는다.** 생기부에서 읽은 값과 학생이 입력한 값이 충돌하면, 입력칸은
  쓸 수 있게 두고 불일치를 알려준다. 학생이 직접 입력한 정보가 AI 추론보다 우선한다.
- **출결은 화면에 노출하지 않는다.** 챗봇이 참고하는 재료로만 쓴다(백엔드에 출결
  API는 있다).
- **업로드한 생기부 원본은 계정에 보관된다**(백엔드 결정 P-1). 보관·삭제를 말하는 화면
  문구가 이와 어긋나면 안 된다. 계정 탈퇴 시 30일 뒤 계정과 모든 기록이 삭제된다.
- **없는 데이터를 지어내지 않는다.** 학생이 입력한 적 없는 성적, 백엔드가 주지 않는
  진행 단계, 계산할 수 없는 통계는 화면에 채우지 않고 비워 둔다.
- **피드백은 append-only 신호다.** 원래 추천·계획 실행 기록을 덮어쓰지 않는다.
- **결과를 보장하지 않는다.** 합격 가능성 예측, 특정 기업 취업 경로 추천은 하지 않는다.
- 결제·구독은 범위 밖이다.

**기술 제약:** 프론트엔드는 LLM을 직접 호출하지 않는다(사용량 한도가 백엔드에만
있다). API 라우트·DB 스키마·파서·프롬프트를 이 리포에 만들지 않는다. API 타입은
백엔드 `/openapi.json`에서 생성한다.

**알려진 미완성:**

- 수시 지원 준비 작업실이 부르는 `/admission-catalog/*`,
  `/application-preparations/*` 엔드포인트가 백엔드 `main`에 아직 없어 이 탭은 지금
  기능하지 않는다. `/education-policies/me`도 같은 이유로 "입학 연도 확인 필요"만
  보인다.
- 소셜 로그인은 Google 하나만 노출한다. Apple과 카카오는 로그인 화면에서 제거했다.

## Brand Commitments

- **이름:** 세특연구소 · Seteuk Lab. 로고 `public/logo.png`(원본 `public/logo_orig.png`),
  파비콘 `public/favicon.svg`.
- **UI 언어:** 한국어.
- **목소리:** 존댓말. 담담하고 과장 없이 쓴다. "당신의 잠재력을 폭발시키세요" 같은 과장
  카피를 쓰지 않는다.
- **판단은 학생이 한다.** 앱은 근거를 보여주고 제안만 한다. 계획 확정, 기록 연결, 추천
  채택은 학생이 직접 누른다.

## Evidence on Hand

- **실사용 수치·후기·합격 사례·요금 정보는 없다.** 누적 이용자 수, 합격률, 후기, 요금은
  검증 가능한 데이터가 생기기 전까지 싣지 않는다(`app/landing-content.ts` 머리 주석).
  예전 랜딩에 있던 가짜 통계와 후기는 제거됐다. 새로 만들어 넣지 않는다.
- 랜딩의 사실 기반 문구: `app/landing-content.ts`(원칙 3개, 기능 3개, FAQ 4개).
- 화면 스크린샷: `docs/screenshots/`(landing, overview, activities, grades, timetable).
- 추천 파일럿 명세와 평가 세트: `docs/semiconductor-pilot-v0.3.md`,
  `docs/semiconductor-evaluation-set-v0.1.md`. 파일럿 학생은 가상 학생이다.
- **실제 학생을 식별할 수 있는 데이터는 샘플·스크린샷·테스트·로그에 넣지 않는다.**

## Product Principles

1. **모든 화면은 루프의 한 단계다.** 새 화면은 진단·계획·실행·기록 중 어디에 속하는지,
   lineage를 어떻게 잇는지 답할 수 있어야 한다.
2. **근거가 먼저, 제안은 그다음.** 추천은 그 학생의 기록에서 나온 근거와 이유를 달고서만
   나타난다. 근거가 없으면 추정하지 않고 비워 두거나 상담으로 넘긴다.
3. **판단은 학생에게 남긴다.** 앱은 제안하고 학생이 확정한다. 중요한 상태 변화는 학생의
   명시적 입력으로만 일어난다.
4. **사실과 해석을 구분한다.** 생기부에서 확인된 사실, 학생이 입력한 값, AI의 해석을 섞어
   보여주지 않는다. 충돌은 막지 않고 알린다.
5. **16세가 혼자 읽을 수 있게 쓴다.** 입시 용어는 풀어 쓰고, 과장 없이 담담하게 말한다.

## Accessibility & Inclusion

- 독해 기준은 16세 고등학생이다. 입시·교육과정 전문 용어를 설명 없이 쓰지 않는다.
- 데스크톱과 모바일을 모두 지원해야 한다.
- 준수할 공식 접근성 기준(WCAG 등급 등)은 아직 정하지 않았다.
