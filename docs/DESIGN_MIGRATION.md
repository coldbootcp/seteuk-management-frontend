# 디자인 마이그레이션 — preview-3200 → 실서비스 프론트엔드

목업(`preview-3200/index.html`, 3200 포트)의 디자인을 실제 서비스(3100 포트)로 옮기는
작업의 계획과 판단 근거를 남긴다. **기능의 정본은 3100이고, 디자인의 정본은 3200이다.**

## 0. 출발점에서 확인한 사실

- 목업은 React 18 + Tailwind(CDN) + Pretendard로 쓰인 **단일 파일 정적 목업**이다.
  데이터는 전부 하드코딩이고 백엔드 호출이 없다.
- 목업 사이드바 주석에 `(Matching 3100 port)`가 적혀 있다 — 목업은 우리 화면을
  기준으로 다듬어 만든 것이라, 전면 재작성이 아니라 **정제**에 가깝다.
- 두 쪽의 색 팔레트가 이미 같다: `#3182F6`(Toss action blue), 텍스트 `#191F28`,
  표면 `#F2F4F6`, 경계 `#E5E8EB`.
- 3100의 `globals.css`는 `--font-body`의 첫 후보로 이미 `Pretendard`를 적어 두고도
  폰트를 내려받지 않아 시스템 폰트로 대체되고 있었다.

## 1. 복구 지점

마이그레이션 시작 전 양쪽 저장소를 커밋·푸시했다.

| 저장소 | 브랜치 | 커밋 |
|---|---|---|
| backend | `feat/planning-chatbot-recommendations` | `4a7de10` 진단+상담 관문 |
| frontend | `feat/backend-integration` | `abbcac2` 상담 관문·시간표·성적 + 미연결 기능 4종 |

되돌리려면 위 커밋으로 `git reset --hard` 하면 된다.

## 2. 전략 — 왜 이 방법인가

세 가지 선택지를 두고 판단했다.

1. **CSS만 갈아끼우기** — 마크업을 그대로 두고 `globals.css`만 목업처럼 고친다.
   위험은 가장 낮지만, 목업에만 있는 구조(상단 검색바, 카드 안 배지, 진행 스트립)는
   재현할 수 없다.
2. **Tailwind로 전면 재작성** — 충실도는 가장 높지만 모든 화면의 JSX를 건드려야 해서
   핸들러가 끊길 위험이 크고, 같은 파일을 동시에 편집 중인 작업과 충돌한다.
3. **혼합(채택)** — 기반을 먼저 깔고, 화면을 하나씩 옮기며 매번 동작을 확인한다.

**Tailwind 유틸리티를 preflight(리셋) 없이 켰다.** 목업 마크업을 번역 없이 옮기기
위해서다. 리셋을 켜지 않은 이유는 `globals.css`가 이미 자체 리셋을 갖고 있어서
그 위에 Tailwind 리셋을 얹으면 기존 6,600줄이 기대하는 기본값이 조용히 어긋나기
때문이다. 클래스 이름 충돌은 없다(우리 CSS의 클래스는 전부 `.dna-card` 같은
의미 이름이고, 유틸리티와 겹치는 이름은 `.btn` 하나인데 Tailwind는 `.btn`을
만들지 않는다).

## 3. 단계 (완료)

- [x] **기반**: Pretendard 로드, Tailwind 유틸리티(리셋 제외) 활성화, 목업 테마
      토큰을 `@theme`으로 이식. 요소 리셋은 `@layer base`로 내렸다 — 레이어 밖에
      두면 `button { background: none }`이 `bg-brand-600` 같은 유틸리티를 이긴다.
- [x] **셸**: 사이드바(브랜드·학생 카드·네비 배지·하단)와 상단바(Academic Hub
      브레드크럼·검색·연동 배지·설정). 탭 순서와 이름을 목업에 맞췄다.
- [x] **이번 학기**: 히어로 + 2열 주제 카드(우선순위 배지·과목 태그·작성 완료) +
      지표 4장 + DNA 카드(그라데이션 서사 + 사실/해석 2열).
- [x] **활동 & 세특**: 1/3 폼 + 2/3 피드. 일치도 배지는 실제 정합 confidence.
- [x] **대시보드(신규)**: 이번 학기 핵심 목표 3장 + 프로필/진행 + 내신 요약.
- [x] **프로필**: 8/4 그리드 + SETEUK PASS 카드(상태 값은 전부 실제 기록에서 셈).
- [x] **성적**: KPI 4카드(연계율·이번 학기 세특은 seteukCount를 실제로 센 값).
- [x] **로그인**: 목업 카드 구성. 구글·애플·비밀번호 찾기는 백엔드가 없어 '준비 중'
      비활성화. "로그인 상태 유지"는 refresh 토큰 저장 여부로 실제 동작한다.
- [x] **상담 게이트**: 단계 배지 + SWOT 4색 리포트.
- [x] **챗봇**: 마크다운 렌더러를 붙여 `**굵게**`가 원문으로 찍히던 문제를 없앴다
      (목업보다 나은 부분).
- [x] **비로그인 랜딩(신규)**: 헤더·히어로·워크스페이스 미리보기·핵심 기능·사례·
      플랜·FAQ.
- [x] **3개년 로드맵 제거**: 소유자 요청으로 화면·탭·링크·전용 헬퍼를 전부 삭제.
      `workspace.roadmap` 데이터는 남겼다 — 이번 학기 목표·주제 제안·대시보드가
      전부 이 데이터를 읽으므로, 지우면 이번 학기 화면이 빈 화면이 된다.
- [x] **온보딩**: 목업의 3화면 구성으로 다시 짰다 — (0) 진단 방식 선택 두 카드,
      (1) Step 1 스테퍼 + 섹션 카드(인적사항·목표·구체도·고민·제약),
      (2) Step 2 AI 확인 질문(파랑·보라·초록 선택 카드). 목업의 "고민 다중 선택"은
      백엔드의 `self_assessed_weaknesses`로 실제 저장된다 — 그 자리는 지금껏 빈 채로
      전송되고 있었다. 목업의 진행률 체크리스트 모달은 **쓰지 않았다**: 생기부 파싱
      진행 상태를 백엔드가 단계로 주지 않아, 체크가 켜지는 것은 전부 지어낸 진행이
      된다. 대신 같은 시각 언어의 비차단 상태 줄로 두어 분석 중에도 폼을 채울 수 있다.
- [x] **진단·상담 관문**: `.consultation-*` 클래스에 CSS가 아예 없어 레이아웃이 없는
      화면이었다. 틀 안으로 넣고 리포트·상담 패널을 목업 카드로 다시 그렸다.
      **진단 사전질문의 선택지를 처음으로 화면에 그린다** — 백엔드가 `options`를
      주는데 화면은 빈 입력칸만 그려서 그 선택지가 통째로 버려지고 있었다.
- [x] **갇히지 않게 하기**: 온보딩과 관문은 사이드바가 없어 로그아웃 버튼이 없었고,
      온보딩을 끝내지 못한 계정은 화면에서 빠져나갈 수 없었다. `GateFrame`이 상단바를
      들고 두 화면이 공유한다.
- [x] **챗봇**: 목업의 1:3 구성(대화 목록 + 대화 창). 말풍선과 입력창은
      `chat-thread.tsx`로 빼내 상담 관문과 함께 쓴다 — 같은 대화를 두 화면이 서로 다른
      모양으로 그리던 것을 없앴다.
- [x] **수시 포트폴리오**: 목업의 2열 카드(자소서 서사 / 면접 질문). 목업은 내용이
      전부 하드코딩이지만 우리는 실제 활동 기록에서 만들므로 카드 모양만 가져왔다.
- [x] **시간표**: 목업의 카드 그리드(요일 헤더 · 오전 · 점심 배너 · 오후 · 하단 요약)와
      우측 패널(학기 선택 · 시간표 목록 · 과목별 주당 시수). 과목 서랍이 "2건 진행 중 ·
      D-12"를 하드코딩하고 있어 실제 활동 기록을 세도록 바꿨다.
- [x] **성적**: 추이·분포 차트를 목업의 2:1 카드로, 학기 탭을 성적표 카드 안의 알약
      탭으로. 화면 폭을 960px로 묶던 컨테이너를 걷어냈다.
- [x] **성적의 지어낸 값 제거**: 목업 성적 40여 과목이 하드코딩돼 있어 기록이 없는
      학생에게도 1~2학년 성적표가 본인 것처럼 채워져 보였다(대시보드는 같은 상황에서
      빈 상태를 보여줘 두 화면이 서로 어긋났다). 함께 있던 다른 세 자리 —
      시간표에서 과목을 가져올 때 2등급/A를 넣던 것, 새 과목 행을 2등급·원점수 90으로
      만들어 그대로 저장하던 것, 교과 구분만 바꿔도 등급을 넣던 것 — 도 모두 비웠다.
- [x] **남은 폼**: 아직 Tailwind로 옮기지 않은 폼(프로필·활동 기록)은 `.form-field`
      규칙 한 곳을 목업 입력칸 모양에 맞춰 함께 톤을 맞췄다.

## 4. 목업에 있지만 백엔드가 받쳐주지 못하는 것

옮기기 전에 판단이 필요한 항목들이다. 화면만 만들면 눌러도 아무 일이 없거나,
사실이 아닌 것을 사실처럼 보여주게 된다.

| 목업 요소 | 상태 | 처리 |
|---|---|---|
| 상단 전역 검색(⌘K) | 검색 엔드포인트 없음 | 화면에 두되 **클라이언트 필터**로 축소하거나 보류 |
| 알림 벨 | 알림 도메인 자체가 없음 | 장식으로 두지 않고 **제외** |
| Pro / VIP 요금제·결제 | `CLAUDE.md`에 "결제·구독은 범위 제외" 명시 | 배지는 **장식 표기**로만, 결제 흐름 없음 |
| "재학생 / 인증" 배지 | 학교 인증 개념 없음 | 정적 문구로 표기 |
| 전교 석차 백분위·전국 수능 대비 | 백엔드에 없는 통계 | **계산 가능한 값만** 표시(평균 등급, 이수 단위) |
| 활동 카드 "일치도 98%" | `reconciliation_logs.confidence`로 대체 가능 | 실제 값 사용 |
| 랜딩의 누적 수치·합격률·후기 | **실제 데이터 아님** (14,800+ / 92.4% / ★4.98 / 합격 사례) | 사실이 아닌 수치를 실서비스에 그대로 쓸 수 없다 — 소유자 확인 필요 |
| 온보딩의 소속 고등학교·고교 유형·목표 대학 | `ProfileRequest`에 해당 필드가 없다 | 넣어도 저장되지 않고 사라지므로 **제외**. 필요하면 백엔드 스키마부터 넓혀야 한다 |
| 생기부 분석 4단계 진행 체크리스트 | 업로드 상태는 pending/processing/done뿐 | 지어낸 진행을 보여줄 수 없어 **비확정 진행 표시**로 대체 |

## 4-1. 아직 남은 것

- 시간표의 **과목 검색 / 직접 추가 모달**과 성적의 **시간표 선택 모달**은 옛 CSS
  (`.tt-bottom-sheet`, `.timetable-picker-panel`)를 그대로 쓴다. 화면 위에 잠깐
  뜨는 오버레이라 톤이 크게 튀지 않아 뒤로 미뤘다.
- 성적표 **테이블 본문의 입력 셀**(`.table-input`, `.table-select`)은 옛 CSS 그대로다.
  카드·탭·헤더만 옮겼다.
- 챗봇 대화 목록에 **상담 대화가 함께 보인다.** 백엔드 `ConversationRead`에
  `purpose`가 없어 화면에서 걸러낼 수 없다 — 거르려면 백엔드 응답부터 넓혀야 한다.

## 4-2. `integration/latest-with-admissions` 병합 (2026-09-14)

Hanjun님이 별도로 진행한 브랜치(9개 커밋, `c7a7cae` 이후로 갈라짐)를 main에 합쳤다.
소유자 지시: "최대한 최신 커밋을 반영하면서, 3개년 흐름도 일단은 만들어줘."

- **새로 들어온 것**: 수시 지원 준비 작업실(`application-preparation-view.tsx`,
  대학·전형 카탈로그 검색 + 공식 모집요강 근거 + 활동 증거 연결), 3개년 흐름 탭
  (`ThreeYearJourney`), 5등급제/9등급제를 입학연도 기준으로 판정하는 성적 화면,
  랜딩의 가짜 통계·후기·요금제 완전 제거, 진단 결과가 비었을 때의 안내 문구.
- **"3개년 흐름" 재도입에 대한 판단**: 예전에 소유자 지시로 삭제한 로드맵 UI는
  `SUBJECT_COLORS`·하드코딩 제안 10개짜리 가짜 타임라인이었다. 이번 `ThreeYearJourney`는
  실제 `workspace.roadmap.nodes`만 읽고 과거(실제 기록)·현재(실행 주제)·미래(방향)를
  구분해 보여준다 — 구조가 달라 병합했다.
- **이번 세션에서 되돌아간 것**: 진단 사전질문 화면(`/diagnosis/pre-questions` 렌더링),
  온보딩 Step 2 "AI 맞춤 확인 질문" 화면. Hanjun님 쪽 최신 판단(확인은 상담 대화
  안에서 그때그때 처리)을 그대로 받았다.
- **grades-view.tsx 충돌 해소**: 하드코딩 샘플 성적 제거(이번 세션)와 5/9등급제
  대응(Hanjun님 쪽)을 모두 살렸다 — 시간표에서 가져온 과목은 성적을 비워 두고,
  기존 입력값은 학생의 등급제 범위로만 clamp하고 새로 지어내지 않는다.
- ⚠️ **`ApplicationPreparationView`가 부르는 `/admission-catalog/*`,
  `/application-preparations/*` 엔드포인트가 우리 백엔드 저장소(`main`)에 없다.**
  실행하면 404가 나고 화면은 "불러오지 못했습니다" 안내로 정상적으로 죽지 않지만,
  이 탭은 그 백엔드 도메인이 합류하기 전까지 기능하지 않는다.
- `/education-policies/me`도 같은 이유로 지금은 항상 실패해 "입학 연도 확인 필요"
  안내만 보인다 — 백엔드가 이 엔드포인트를 갖추면 그대로 동작한다.

## 5. 원칙

- **기능이 먼저다.** 디자인을 옮기다 핸들러·상태·API 호출이 끊기면 그 이식은 실패다.
  화면 하나를 옮길 때마다 실제로 눌러 보고 넘어간다.
- **없는 데이터를 지어내지 않는다.** 목업의 하드코딩 수치는 실제 값으로 바꾸거나,
  값이 없으면 그 자리를 비운다.
- **동시에 편집 중인 파일을 존중한다.** 성적·시간표 화면은 다른 작업에서 만들어지고
  있으므로 구조를 갈아엎지 않고 톤만 맞춘다.
